/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['via.placeholder.com', 'nodeadmin.innobaytsolutions.com', 'localhost'],
      },
}

module.exports = nextConfig
